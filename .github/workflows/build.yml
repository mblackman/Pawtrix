name: Build pawtrix

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build --configuration Release --no-restore
      - name: Zip Directory Linux
        if: runner.os == 'Linux'
        run: zip -r pawtrix-${{ runner.os }}.zip pawtrix/bin/Release/net8.0
      - name: Zip Directory Windows
        if: runner.os == 'Windows'
        run: Compress-Archive -Path pawtrix/bin/Release/net8.0/* -DestinationPath pawtrix-${{runner.os}}.zip
      - uses: actions/upload-artifact@v4
        with:
          name: pawtrix-${{ runner.os }}
          path: pawtrix-${{runner.os}}.zip